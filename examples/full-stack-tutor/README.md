# Full-Stack Tutor

Small vanilla browser plus Express server example for a tutoring app.

```bash
npm install
npm test
npm run dev
```

Open <http://localhost:3001>.

The example includes the full project-owned Agent Workflow scaffold under `.agents/`.
