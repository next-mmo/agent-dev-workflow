# Agent Memory Store

The `.agents/docs/memory/` directory stores concise, reusable learnings, conventions, and decisions for local context recall.

Use `NNNN-kebab-case-title.md` with YAML frontmatter. Keep entries verified and specific; drafts and placeholders are excluded from native recall.
